node_modules/
lib/
dist/
coverage/
# Rust build artifacts
target/
*.rs.bk
# prebuilt/ is intentionally NOT ignored: prebuilt/<triple>/dsh_powerdesk_pty.node
# is committed so git-hosted installs work without a local Rust toolchain.
# Local env
.env
.env.local
.DS_Store

# pnpm content-addressable store (sandbox artifact, not source)
.pnpm-store/
